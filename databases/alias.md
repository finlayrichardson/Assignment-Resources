Aliases allow you to name a field in your output.

Example:

```sql
SELECT firstName AS "First name"
FROM people;
```

Note: the alias name requires square brackets or double quotes if the alias contains a space.